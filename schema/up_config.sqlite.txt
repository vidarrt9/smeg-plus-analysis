CREATE TABLE [UP_Keys] ([Section] VARCHAR NOT NULL, [Name] VARCHAR NOT NULL, [Type] INTEGER NOT NULL DEFAULT 0, [Idx] INTEGER NOT NULL DEFAULT 0, [IntValue] INTEGER, [FloatValue] REAL, [StringValue] VARCHAR, [BlobValue] BLOB, [reset_factory_enabled] INTEGER NOT NULL DEFAULT 0, [modified] INTEGER DEFAULT 0, PRIMARY KEY ([Section], [Name], [Idx]));
